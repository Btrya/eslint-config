{
  "name": "eslint-config-btrya",
  "version": "0.0.0",
  "description": "quick to use Btrya's eslint-config, minimal configuration but supports custom configuration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Btrya",
  "license": "ISC",
  "private": "true",
  "workspaces": [
    "package/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
